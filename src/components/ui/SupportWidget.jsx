// SupportWidget is now integrated directly into the luxury Header component
// as a floating WhatsApp pill for better global accessibility and performance.
// Returning null to prevent duplicate renders across legacy pages.
const SupportWidget = () => null;

export default SupportWidget;